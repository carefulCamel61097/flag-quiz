{
  "name": "flag-quiz",
  "version": "0.1.0",
  "private": true,
  "description": "Programmatically generated flag quizzes (colour pie charts, inverted flags, zoomed crops).",
  "type": "module",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "build": "npm run build:fame && npm run build:flags && npm run build:colors && npm run build:crops && npm run build:fakes && npm run build:mosaics && npm run build:filters && npm run build:lookalikes",
    "build:fame": "node scripts/build-fame.mjs",
    "build:flags": "node scripts/build-flags.mjs",
    "build:colors": "node scripts/build-colors.mjs",
    "build:crops": "node scripts/build-crops.mjs",
    "build:fakes": "node scripts/build-fakes.mjs",
    "build:mosaics": "node scripts/build-mosaics.mjs",
    "build:filters": "node scripts/build-filters.mjs",
    "build:lookalikes": "node scripts/build-lookalikes.mjs"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "flag-icons": "7.5.0",
    "world-countries": "5.1.0"
  }
}
